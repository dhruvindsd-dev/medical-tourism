import { OPACITY_VARIANTS, OPACITY_VARIANTS_DELAY } from "./variants"

export const ADD_OPACITY_ANIMATION = {
	initial: "initial",
	animate: "animate",
	exit: "exit",
	variants: OPACITY_VARIANTS,
}

export const ADD_OPACITY_ANIMATION_DELAY = {
	variants: OPACITY_VARIANTS_DELAY,
	initial: "initial",
	animate: "animate",
	exit: "exit",
}
